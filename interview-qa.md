1. Вопрос про архитектуру проекта, на котором работал.

2. В каких случаях Вы применяете parallelStream() для обработки коллекции? 
Какие условия необходимы, чтобы получить реальный прирост производительности, и с какими подводными камнями Вы сталкивались на практике в production коде?

3. Заказчик просит создать высоконагруженный микросервис на Spring Boot. Требования: REST API, асинхронный обмен через Kafka, работа с PostgreSQL, аутентификация по токенам. 
Какие стартеры Spring Вы бы использовали и как их настроить для высокой пропускной способности и отказоустойчивости. 
Какие дополнительные библиотеки/подходы (кеширование, мониторинг, транзакционность) Вы примените, и как они повлияют на архитектуру? 
Расскажите про потенциальные узкие места и способы их устранения.

4. Как используются кибана и графана при поиске ошибок в микросервисах.
   Каким образом можно отправлять дополнительные собственные метрики в кибану?

5. Какой у Вас опыт работы с нереляционными базами данных (Mongo, Cassandra ElasticSearch)? Приходилось ли писать запросы для поиска, напрямую или через библиотеки в коде?

6. Какие задачи с кафкой приходилось решать?
   Настройки многопоточности для получения данных из топика

7. Представим, что на проекте реализована передача документов между двумя сервисами через rest. Оба микросервиса подключены к одной базе данных.
Так как REST не самое надежное соединение, то периодически документы не доходят
Каким образом вы бы обеспечили доставку сообщений без потерь?


----------------------

Ниже — реальные формулировки «вопросов на подумать» (про проектирование/архитектуру), которые встречались на Java-собеседованиях из базы (System Design / микросервисы / интеграции / транзакционность / надежность).

System design / микросервисы
• «Выбор между микросервисами и монолитом. Плюсы и минусы каждого»
• «Разница между монолитом и микросервисом»
• «Почему все переходят на микросервисы?»
• «Какие методы коммуникации между микросервисами знаешь?»
• «Между микросервисами только синхронное взаимодействие?»
• «(к) У вас БД на каждый микросервис?»
• «Как разделены микросервисы на проекте? С точки зрения бизнес‑функционала»
• «Ситуация. Есть инстанс юзеров. Два микросервиса… при создании юзера роль не проставилась, юзер удалился… до ошибки кто‑то запросил всех юзеров. Как избежать ситуации, чтобы возвращались полностью сконфигурированные юзеры?»


Kafka / очереди / надежная доставка (архитектурные кейсы)
• «Как убедиться, что у нас записалось в Kafka сообщение?»
• «Как убедиться, что сообщение гарантированно доставлено?»
• «Гарантия доставки сообщений в Kafka» / «Гарантии доставки в Kafka»
• «Отличия Kafka и RabbitMQ»
• «Что такое partitions в Kafka? Для чего они?»
• «Давай пройдем путь Producer‑Kafka‑Consumer… партиции, брокера, топики. Как записывает, какие кейсы могут быть?»
• «В каком случае сообщение на стороне consumer'а может обработаться несколько раз?»
• «Ситуация. У вас REST‑сервис, который можно вызвать одновременно не больше пяти раз (иначе 429). Как спроектируете систему если у вас есть Kafka? Как настроить consumer, чтобы не превышал количество разрешенных вызовов?»
• «Ситуация. Есть Kafka и внешний REST‑сервис. Требование: одновременно не больше 3 параллельных вызовов, иначе 429. Как организовать консюмеров, чтобы выполнить требование?»
• «Как producer может узнать, что Kafka упала?»
• «Kafka vs Rest» / «Почему REST будет надежнее Kafka для подтверждения доставки сообщения или нет?»

Транзакции, консистентность, outbox (архитектура данных)
• «Сетевой вызов в транзакции. Чем это чревато?»
• «Сетевой вызов в транзакции в методе addChildren(). Как это исправить?»
• «По задаче… не проблема ли, что @Transactional стоит над методом, в котором происходит вызов по REST’у?»
• «Паттерн transactional outbox» / «Что из себя представляет transactional outbox?»
• «У нас в методе save есть отправка по Kafka… в какой-то момент произойдет ошибка, а мы уже отправили сообщение. Что делать?» (по смыслу — согласование БД и брокера)
• «Ситуация (outbox): записали в outbox, listener отправил в Kafka, но не смог пометить… после рестарта снова доставляет — разве это exactly once?»

Производительность / БД / индексы (как дизайн‑решение)
• «Какой индекс выбрать для таблицы?»
• «Если вешаем индекс на два поля, порядок полей важен?»
• «Имеет ли смысл накидывать индекс на колонку boolean?»
• «Ситуация: таблица с полом (м/ж). В какой ситуации стоит навесить индекс на это поле?»
• «В каких случаях индексы бывают вредны?»
• «Навесили индексы, но не хватает производительности — что еще делать?»
• «Может ли быть, что индекс повесили, но EXPLAIN ANALYZE показывает, что индекс не используется?»
• «Как понять, что индекс используется? Как это отследить?»

Отказоустойчивость / деградация / инфраструктурные паттерны
• «Паттерн Circuit Breaker»
• «Как сделать Circuit Breaker на уровне инфраструктуры?»
• «Расскажите что делали на проекте с Grafana и Prometheus»
• «Если настроить мониторинг без бизнеса? Например количество запросов, время, зависло?»
• «Какие метрики ты бы смотрел на графане?»


----------------------
10-06-2026 - Себеседование в IBS

1. Как обеспечивали exectly once
2. Как обеспечить порядок сообщений
3. Есть проблемы с памятью, но heap в порядке
4. Есть задержка в запросах. Что делать?
5. Есть таблица с миллионом записей. Как вставить поле с значением по умолчанию не останавливая прод?
6. В чем проблема виртуальных потоков?
7. Как уменьшили вирутальную память на 15%?
